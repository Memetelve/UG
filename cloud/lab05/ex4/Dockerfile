FROM alpine:latest

ARG PYTHON_VERSION=3.13
ARG SERVER_PORT=4200

ENV PYTHON_VERSION=${PYTHON_VERSION}
ENV SERVER_PORT=${SERVER_PORT}

VOLUME /data

CMD echo "Python version: $PYTHON_VERSION, Server Port: $SERVER_PORT"
